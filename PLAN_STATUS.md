# Flutter Forge Plan Status

这份文档记录当前这轮重构已经完成了什么、下一阶段还要做什么，以及整体规划。它是当前正式的改造中书，放在项目根目录，替代已经删除的迁移路线图入口。

## 当前目标

把 `flutter-forge` 从“旧的多角色顺序流转 + 强接管思路”收口为：

- 用户显式触发优先
- `controller` 主控
- 双轨模型：`C0-C3` / `S0-S6`
- 按需专项判断
- 大任务可拆包并行
- 任务完成即退出，不跨轮常驻接管

## 已完成

### 1. 主协议重写

已完成：

- `SKILL.md` 改成单一主协议
- 拆清 `命中路由 / 任务分类 / 工作模式 / 输出日志 / 执行协议`
- 任务类型与任务规模分离
- 优化场景拆成：
  - `需求 + 优化`
  - `UI 优化`
  - `架构优化`

### 2. 执行模型重写

已完成：

- 引入 `controller` 主控
- 引入双轨：
  - `C0-C3` 新项目共创
  - `S0-S6` 执行轨道
- 引入大任务并行协议：
  - `arch-agent` 拆包
  - 多 `impl-agent` 并行
  - `verify-agent` 收口

### 3. 角色卡重写为代理契约

已完成：

- `requirement_analyst.md`
- `ui_designer.md`
- `architecture_designer.md`
- `page_engineer.md`
- `verify_agent.md`

这些文件现在都明确了：

- 输入
- 输出
- 可决定范围
- 必须回传主控的条件
- 禁止事项

### 4. 关键运行时 reference 已重写

已完成：

- `task_runtime_prompt.md`
- `skill_visibility.md`
- `session_management.md`
- `memory_protocol.md`
- `rule_card_protocol.md`
- `quality_gates.md`
- `startup_handshake.md`
- `project_init_flow.md`
- `new_project_cocreation_mode.md`
- `official_flutter_skills.md`
- `role_handoff_formats.md`

### 5. 示例与入口已切到新协议

已完成：

- `example_workflow.md`
- `existing_project_entry.md`
- `new_project_app_profiles.md`
- `case_studies.md`

这些文档已不再使用：

- `迭代中项目扫描`
- `新项目初始化`
- `需求理解 -> 设计包`

### 6. 触发与记忆策略已收口

已完成：

- 改为显式触发优先
- 不再因为“当前是 Flutter 项目”而默认强接入
- `session` 只服务当前未完成大任务
- 任务完成时立即重置 `session`
- 新一轮任务默认不恢复旧状态
- 大任务结束时再询问是否刷新规则卡

### 7. 迁移路线图已退场

已完成：

- 删除 `references/roadmaps/controller_agent_v1_v2_plan.md`
- 从 `SKILL.md`、`README.md`、`load_map.md` 中移除路线图入口

## 当前状态判断

可以认为当前已经完成了：

- `M0`
- `M0.5`
- 主协议层的 `v1 / v2` 落地

目前项目状态更接近：

- 一套已经可运行的 skill 文档协议

而不是：

- 还停留在设计稿状态的迁移计划

## 下一阶段还需要做什么

### P1. 低频历史文档继续清扫

虽然主入口与高频引用已经切干净，但 `references/` 里仍可能存在少量低频历史文档，带有旧心智、旧命名或过时示例。

目标：

- 再扫一轮低频文档
- 只保留现行术语
- 删除对模型没有价值的迁移解释

优先级：中

### P2. README 再压缩

当前 README 已经同步新协议，但“使用”与“架构说明”部分还可以再缩一版，进一步强化：

- 推荐触发方式
- 两个最常用开场模板
- 当前真正推荐的使用方式

目标：

- 减少用户阅读负担
- 让 README 更贴近现在的执行心智

优先级：中

### P3. 规则卡刷新流程细化

现在规则已经改成“大任务结束时询问是否刷新规则卡”，但还可以继续细化：

- 哪些变更必须建议刷新
- 哪些变更只需提示可选刷新
- 刷新时输出模板

优先级：中

### P4. 做一次完整一致性回归检查

目标：

- 再扫一遍 `SKILL.md + README + references`
- 检查是否还有口径冲突
- 检查是否还有引用漂移
- 检查是否还有旧术语回流

优先级：高

### P5. 如无新增规则需求，进入稳定期

如果上面几项都完成，下一步就不再做结构重构，而是进入：

- 稳定维护
- 真实场景试跑
- 按使用反馈做局部修补

优先级：高

## 整体规划

### 阶段 A：协议收口

目标：

- 把旧协议全部收成新协议
- 删除迁移期说明
- 删除路线图依赖

状态：

- 已基本完成

### 阶段 B：一致性回归

目标：

- 全仓文档口径统一
- 没有旧模式名残留
- 没有“主协议一套、次级文档另一套”的冲突

状态：

- 进行中

### 阶段 C：试运行与反馈

目标：

- 真实任务试跑
- 验证触发、路由、双轨、拆包、验证链是否顺手
- 根据真实使用反馈继续修

状态：

- 未开始

### 阶段 D：稳定维护

目标：

- 只按真实问题做小修
- 不再大规模翻主协议
- 把 `flutter-forge` 维持成稳定可用的 skill

状态：

- 未开始

## 当前建议

如果继续推进，建议按这个顺序做：

1. 做最后一轮低频文档清扫
2. 做一次完整一致性回归检查
3. 压缩 README 的“使用”部分
4. 开始真实任务试跑

## 备注

当前仓库还有一批未提交的文档改动，主要集中在：

- `SKILL.md`
- `README.md`
- `references/`

这属于本轮协议重构的正常结果，不是额外异常变更。
